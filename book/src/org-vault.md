# The Org vault

Atrium can mirror task state to a directory of `.org` files (default
`~/Tasks/`) for editing in Emacs / Doom / vim-orgmode. The discipline
is **DB canonical, vault projected**. The full round-trip contract:

{{#include ../../docs/org-roundtrip.md}}
